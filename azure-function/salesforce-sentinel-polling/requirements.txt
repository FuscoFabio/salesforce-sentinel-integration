# Azure Functions Python dependencies
azure-functions>=1.18.0
azure-functions-worker>=1.0.0

# HTTP requests
requests>=2.31.0

# Date/time handling
python-dateutil>=2.8.2

# JWT auth per Salesforce
PyJWT>=2.8.0

# Azure Storage (Table) per checkpoint persistente
azure-data-tables>=12.5.0

# grpcio: pinning versioni con wheel pre-compilati per Python 3.13
# Usa --only-binary per forzare wheel pre-compilati e evitare compilazione
# Se questa versione non funziona, considera di downgrade a Python 3.12
grpcio>=1.62.2,<2.0.0
grpcio-tools>=1.62.2,<2.0.0
